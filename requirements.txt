# Python 3.13 (tested with 3.13.13). Install with:
#   uv venv && uv pip install -r requirements.txt
# Versions are the ones the app and analysis scripts were run and tested with.

# App
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32

# Single-cell analysis (app and analysis/ scripts)
scanpy==1.12.4
anndata==0.13.4
numpy==2.5.3
pandas==3.0.6
scikit-learn==1.9.1
igraph==1.0.0
